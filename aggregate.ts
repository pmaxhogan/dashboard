/*
const pipeline = [
    {
        groupBy: "$timestamp",
        buckets: 100,
        output: {
            "hour": {"$avg": "$stats.stats.clock.hours"},
            "minutes": {"$avg": "$stats.stats.clock.minutes"},
            "seconds": {"$avg": "$stats.stats.clock.seconds"},
        }
    }
]*/
